<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chiikawa</title>
</head>
<body>
    <%- include('../partials/navbar') %>
    <h1>Edit <%= character.name %></h1>
    <form action="/characters/<%= character._id %>?_method=PUT" method="post">  
        <div>
            <label for="name">Name:</label>
            <input type="text" name="name" id="name" value="<%= character.name %>" />
        </div>
        <div>
            <label for="color">Color:</label>     
            <input type="text" name="color" id="color" value="<%= character.color %>" />
        </div>
        <div>
            <label for="description">Description:</label>  
            <input type="text" name="description" id="description" value="<%= character.description %>" />
        </div>
        <div>
            <label for="chiikawaImage">Image URL:</label>
            <input type="url" name="chiikawaImage" id="chiikawaImage" value="<%= character.chiikawaImage %>" />
        </div>
        <button type="submit">Update</button>
    </form>
    <a href="/characters">Back to Characters</a>
</body>
</html>